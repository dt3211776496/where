{
  "crons": [{
    "path": "/api/cleanup",
    "schedule": "0 0 * * *"
  }],
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
} 